#include<bits/stdc++.h>
#define ll long long int
using namespace std;

ll fct(ll n){
    ll fact = 1;
    while(n!=1){
        fact*=n;
        n--;
    }
    return fact;
}

int main(){
    ios::sync_with_stdio(false);cin.tie(NULL);cout.tie(NULL);
    ll t = 1;
    cin>>t;
    while(t--){
        string a;
        cin>>a;
        ll len = a.length();
        for()

    }
}